# Test Definitions

`infra/aws/assets/test-definitions` contains YAML files that the runner uses to execute a test run inside an existing lab.

Each definition selects:

- deployment profiles for the app and stubs
- load-test runtime settings such as shards, rates, and load profile
