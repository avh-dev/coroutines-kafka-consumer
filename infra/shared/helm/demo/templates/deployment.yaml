apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Release.Name }}
    spec:
      containers:
        - name: demo
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: {{ .Values.env.springProfilesActive | quote }}
            - name: KAFKA_ENABLED
              value: {{ .Values.env.kafkaEnabled | quote }}
            - name: DEMO_KAFKA_ENABLED
              value: {{ .Values.env.demoKafkaEnabled | quote }}
            - name: DEMO_KAFKA_BOOTSTRAP_SERVERS
              value: {{ .Values.env.bootstrapServers | quote }}
            - name: SPRING_DATA_REDIS_HOST
              value: {{ .Values.env.redisHost | quote }}
            - name: SPRING_DATA_REDIS_PORT
              value: {{ .Values.env.redisPort | quote }}
            - name: MODEL_BASE_URL
              value: {{ .Values.env.modelBaseUrl | quote }}
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 15
{{- with .Values.resources }}
          resources:
{{ toYaml . | indent 12 }}
{{- end }}
