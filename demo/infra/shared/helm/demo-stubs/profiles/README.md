# Demo Stubs Helm Value Profiles

Stub behavior is configured dynamically through `POST /settings`.
The environment overlays only size the deployment; stub behavior remains part
of the shared resolved test definition.
