global:
  scrape_interval: ${prometheus_scrape_interval}
  evaluation_interval: ${prometheus_evaluation_interval}

# Runner observability stores metrics pushed from disposable labs through
# Prometheus remote_write. The container service keeps the historical
# datasource uid/name as "prometheus", but the storage engine is VictoriaMetrics.
scrape_configs: []
